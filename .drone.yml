kind: pipeline
type: docker
name: default
   
steps:
- name: build
  pull: always
  image: golang:alpine
  commands:
    - apk update && apk --no-cache add git dep ca-certificates musl-dev gcc build-base
    - go build -o ./build/_output/bin/kafka-autoconnector -gcflags all=-trimpath=/drone/src -asmflags all=-trimpath=/drone/src github.com/walmartdigital/kafka-autoconnector/cmd/manager

- name: docker
  pull: always
  image: plugins/docker
  settings:
    dockerfile: build/Dockerfile
    repo: chindou/kafka-autoconnector
    tags:
      - latest
      # - ${DRONE_TAG}
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    trigger:
      branch:
      - master
      event:
      - push
      - tag
  